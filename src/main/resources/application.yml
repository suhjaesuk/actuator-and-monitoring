management:
  # ??, OS, env ??
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  server:
    port: 8080
  endpoint:
    shutdown:
      enabled:
        true
    health:
      show-details: always
    # show-components: always
  endpoints:
    web:
      exposure:
        include: "*"

info:
  app:
    name: actuator-app
    company: suhjaesuk-company

logging:
  level:
    hello.actuator.controller: debug


server:
  tomcat:
    mbeanregistry:
      enabled: true